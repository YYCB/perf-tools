# Comparison Report Template

> Replace placeholders. The bulk of the body is auto-generated by
> `scripts/compare.py`; this template is for the **narrative** sections you
> add on top.

---

# `<Platform A>` vs `<Platform B>` — `<YYYY-MM>`

**TL;DR**: 1–2 sentences. Verdict + biggest delta.

## Test setup

- Date: `<YYYY-MM-DD>`
- Operator: `<your name>`
- Workload bundles run: `<quick | full | custom>`
- Source data: `results/<A>/<run-id>/`, `results/<B>/<run-id>/`

## Fairness Statement

(auto-generated from `env.json` by `compare.py` — see the generated section
below; flag any ⚠️ row that may bias the comparison.)

## Results

(auto-generated table by `compare.py`)

## Interpretation

For each significant delta, classify the bottleneck:

| Metric | Δ | Bottleneck class | Evidence | Action |
|---|---|---|---|---|
| `<metric>` | `<+x%>` | hardware / driver / SDK / app / config | `<which lower-level metric supports this?>` | `<what to do>` |

Bottleneck classes:

- **hardware** — limited by silicon (cores, memory bandwidth, NPU TOPS)
- **driver / SDK** — limited by vendor stack (CUDA, HBDK, RKNN runtime)
- **OS / kernel** — RT scheduling, IRQ handling, governor
- **middleware** — DDS / RMW / QoS choices
- **application** — model architecture, algorithmic complexity

## Recommendations

- [ ] Action 1 — owner / due date
- [ ] Action 2 — owner / due date

## Open questions

- ?

## Appendix

- Raw env files: `env.json` in each run dir
- Raw benchmark logs: `*.raw.txt` in each run dir
